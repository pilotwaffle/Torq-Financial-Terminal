# Polygon.io / Massive.com — primary data source
POLYGON_API_KEY=your_polygon_key_here

# Finnhub — fallback for news
FINNHUB_API_KEY=your_finnhub_key_here

# Financial Modeling Prep — free tier, used for gainers/losers/most-active
FMP_API_KEY=your_fmp_key_here

# SEC EDGAR — no key, but requires a contact header
SEC_USER_AGENT=TORQ News Reader your@email.com

# Supabase — reuses existing TORQ project
NEXT_PUBLIC_SUPABASE_URL=https://npukynbaglmcdvzyklqa.supabase.co
SUPABASE_SERVICE_ROLE_KEY=get_from_supabase_dashboard

# Cron shared secret — any random string; must match Vercel env
CRON_SECRET=generate_a_long_random_string
